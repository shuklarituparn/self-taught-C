# include <stdio.h> //Don't leave space between the angled bracked and command

int main() {

 _Bool bool1 = 1; // never assign any value greater than 1 to bool or it will print 1 always
 _Bool bool2 = 0; //since they are assined integer values we can print them using %d

  printf( " The value of bool is %d %d", bool1, bool2);
  return 0;

}